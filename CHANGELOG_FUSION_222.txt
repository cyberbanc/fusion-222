FUSION-222 CHANGELOG
====================

v1.0.5 — NO BREAKER
- Removed 3 LOSS -> skip next 3 eligible signals from forward trading.
- Removed breaker from retro replay.
- Fixed stake remains $22.
- EV>=2%, selected probability>=53%, payout-ready, Shadow Recent, Quality PF>=0.85 remain unchanged.
- Quality WR remains display-only.
- New isolated tables: fusion222_v1366_nobreaker_decisions/state/snapshots.
- New tables force clean retro initialization and preserve v1.0.4 as archive.
- Retains v1.0.4 startup hardening, DB auto-detection and cursor-shape fixes.

FUSION-222 v1.0.4 STARTUP HARDENING

- Fixed Railway startup KeyError: 0 caused by indexing a RealDictCursor row with [0].
- Added cursor-shape-agnostic scalar extraction for tuple and mapping rows.
- Hardened every scalar fetch in db.py so the same cursor mismatch cannot recur elsewhere.
- Fixed latent JSONB ALTER TABLE defaults: fragments now use valid '{}' JSON because they are inserted without a second sql.SQL.format pass.
- Keeps DB autodetection, v1.3.6.6-only retro replay, shared main Fusion DB read-only history, private FUSION-222 tables, fixed $22 and the new filters.
- Added regression tests for tuple/RealDict cursor shapes and static startup hazards.

FUSION-222 CHANGELOG

v1.0.3 — SQL/JSONB startup hotfix
- Fixed psycopg2.sql.SQL.format() collision with literal JSONB DEFAULT '{}' values.
- Escaped literal braces in CREATE TABLE templates for decisions and snapshots.
- Keeps DB autodetect, v1.3.6.6-only retro scope, FIXED $22, and existing private table names unchanged.
- Added regression test that runs init_db() SQL composition without a live PostgreSQL connection.

====================

v1.0.2 — corrected retro scope
- Dashboard retro accounting now replays ONLY strategy_version=1.3.6.6.
- ALL_VERSIONS is excluded from bank/PnL/PF/DD initialization.
- Retro cutoff = latest settled 1.3.6.6 base decision at first boot.
- Timer anchor = first stored 1.3.6.6 decision.
- Fixed stake remains $22.
- Strategy remains EV>=2%, selected probability>=53%, payout-ready, Shadow Recent, Quality PF>=0.85, Quality WR display-only, breaker 3 losses -> skip 3 eligible signals.
- Main fusion-ev paper_decisions remains read-only.
- Corrected private tables are fusion222_v1366_decisions, fusion222_v1366_state, fusion222_v1366_snapshots.
- Separate table names prevent accidental reuse of the earlier draft state that had ALL_VERSIONS retro accounting.

HOTFIX v1.0.2
- Fixed startup failure when the main Fusion DB physical history table is not literally named paper_decisions.
- BASE_DECISIONS_TABLE=auto now uses the same legacy-compatible table discovery approach as the main Fusion bot.
- Private fusion222_v1366_* tables are explicitly excluded from discovery.
- Health/status report the actually resolved base table name.
